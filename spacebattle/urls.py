"""URL configuration for the spacebattle application.

Release A ships schema only; Release B adds the REST endpoints and
Release C adds the page views.
"""

from django.urls import path

app_name = "spacebattle"

urlpatterns = []
